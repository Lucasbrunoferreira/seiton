import { mapActions, mapGetters } from 'vuex'
import HeaderBar from '../../components/header-bar'

export default {
  components: {
    HeaderBar
  },
  data () {
    return {

    }
  },
  computed: {
    ...mapGetters([

    ])
  },

  methods: {
    ...mapActions([

    ])
  }
}
